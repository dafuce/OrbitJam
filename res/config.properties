# Config file for Orbit Jam
# Do not change keys (left hand side variables) only their values

# Commas (,) and special characters will result in game crash
# Max length about 20 characters
playerName = edit_config.properties